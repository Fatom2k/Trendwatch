# TrendWatch dependencies
# Python 3.11+

# Anthropic SDK
anthropic>=0.25.0

# HTTP client
httpx>=0.27.0

# Google Trends (4.9.2+ has urllib3 compatibility issues; pin to 4.9.2 with urllib3<2.0)
# OR use latest pytrends that supports urllib3 2.x
requests>=2.31.0
urllib3<2.0.0
pytrends>=4.9.2

# Scheduling
APScheduler>=3.10.4

# NLP / Clustering
scikit-learn>=1.4.0
numpy>=1.26.0

# Elasticsearch (pin to 8.x for compatibility with ES server 8.13.0)
elasticsearch>=8.0.0,<9.0.0

# Web interface (FastAPI + auth)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# Jinja2 3.1.4 has a cache-key bug with Starlette (unhashable dict).
# Pin to 3.1.3 until upstream fixes it.
jinja2==3.1.3
python-multipart>=0.0.9
authlib>=1.3.1
itsdangerous>=2.2.0

# YouTube Data API v3
google-api-python-client>=2.120.0

# Config
python-dotenv>=1.0.1
PyYAML>=6.0.1

# Testing
pytest>=8.1.0
pytest-cov>=5.0.0

# Optional: sentence-transformers for richer clustering (Phase 3)
# sentence-transformers>=3.0.0
